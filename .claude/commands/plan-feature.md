---
description: Plan a feature with impacted files, risks, and validation strategy.
disable-model-invocation: true
---

Follow `AGENTS.md` as strict policy.

Task: plan the feature "$ARGUMENTS".

Return:

1. Scope and assumptions.
2. Impacted files (exact paths).
3. Implementation steps in order.
4. Risks and mitigations.
5. Test plan with targeted Playwright command(s).
6. Validation commands (`npm run lint` and build guidance).

Do not edit files in this command.
