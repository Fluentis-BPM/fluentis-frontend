---
description: Add or update typed API helper using shared axios patterns.
disable-model-invocation: true
---

Follow `AGENTS.md`.

Implement API helper for: "$ARGUMENTS".

Requirements:

- Use shared client from `src/services/api.ts` only.
- Define request/response types near the helper.
- Handle errors with actionable messages.
- Preserve existing route fallback conventions if relevant.

Run `npm run lint` and report changed files.
